// Create a Date object for right now, then log the year, month and day separately.

const myDate = new Date()

console.log(myDate.getFullYear());
console.log(myDate.getMonth() + 1);
console.log(myDate.getDate());